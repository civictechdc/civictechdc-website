markdown: kramdown
name: Civic Tech DC
url: https://civictechdc.org
title: Civic Tech DC
description: "We are a non-partisan volunteer community in the DMV using open-source technology to support civic engagement, strengthen democracy, and empower public-interest initiatives locally and nationally."
encoding: utf-8
timezone: America/New_York
plugins:
  - jekyll-redirect-from
  - jekyll-archives
future: true
baseurl: ""

# Keep blog posts under /blog/ regardless of their categories.
# Scoped to posts only (via defaults) so the global permalink style is unchanged
# and projects/events/pages keep their existing URLs. Without this, Jekyll's default
# scheme prefixes a post's categories to its URL (e.g. a "Community" post lands at
# /community/..., colliding with the Community page).
defaults:
  - scope:
      path: ""
      type: posts
    values:
      permalink: /blog/:year/:month/:day/:title/

collections:
  projects:
    output: true
  events:
    output: true
    permalink: /events/:name/

# Jekyll Archives for tag and category pages
jekyll-archives:
  enabled:
    - categories
    - tags
  layout: archive
  permalinks:
    category: "/blog/category/:name/"
    tag: "/blog/tag/:name/"

exclude:
  - /node_modules/
  - /.playwright-mcp/
  - gulpfile.js
  - Gemfile
  - Gemfile.lock
  - npm-debug.log
  - /sass
  - README.md
  - CONTRIBUTING.md
  - CLAUDE.md
  - package.json
  - package-lock.json
